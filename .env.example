# Optional Vite dev settings only (gitignored when copied to `.env`).
# Secrets belong in `.dev.vars` — not here.
#
# Copy to `.env` when accessing the dev server through Tailscale or a tunnel:
# VITE_ALLOWED_HOSTS=your-hostname.example.com,.tailscale.net
